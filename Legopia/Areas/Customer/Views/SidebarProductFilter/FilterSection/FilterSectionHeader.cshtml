@model FilterSectionModel

<div class="d-flex justify-content-between align-items-center mb-2">
    <strong>@Model.Title</strong>
    <button class="btn btn-sm btn-link toggle-section" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-@Model.SectionKey" aria-expanded="true" aria-controls="collapse-@Model.SectionKey">
        <i class="bi bi-chevron-down"></i>
    </button>
</div>
